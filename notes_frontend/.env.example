# Frontend public environment variables (must start with PUBLIC_ to be exposed in client code)

# PUBLIC_INTERFACE
# PUBLIC_API_BASE_URL is the base URL of the backend notes service, e.g.:
# http://localhost:8080/api
# If omitted, the app will use a localStorage-backed mock for notes.
PUBLIC_API_BASE_URL=
